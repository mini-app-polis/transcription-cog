"""Prompt templates loaded at runtime by the clients.

Templates are stored as ``.md`` next to this file so the package data
(see [tool.hatch.build.targets.wheel.force-include] in pyproject.toml)
ships them in the wheel. The canonical wording / tuning notes live in
``docs/PROMPT.md`` — keep them in sync.
"""
